"""Private organization package for public declarations.

Why:
    Keeps caller-facing declarations grouped by behavior while the top-level
    package remains the only supported import entrypoint.
"""
